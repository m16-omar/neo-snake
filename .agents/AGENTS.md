# Project-Scoped Rules for Neon Snake

- **Always Push Changes**: Every time a file modification is completed, immediately run the corresponding Git commands to stage, commit, and push the changes to the remote repository.
  - Command pattern:
    1. `git add .`
    2. `git commit -m "commit message explaining the changes"`
    3. `git push`
